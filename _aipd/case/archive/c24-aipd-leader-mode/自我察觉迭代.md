# 自我察觉迭代：归档 Case 后复用分支

## 暴露的问题

- C23 已经完成并归档，但后续 Leader V2 功能继续在 `codex/c23-versioned-aipd-update` 分支上开发。
- 分支名让当前对话产生“这仍然是 C23”的隐性认知，实际新功能没有在执行前建立独立 Case。
- 到最终收尾时，Case 索引只显示无关的 C19 在进行中；若机械选择唯一 active Case，会错误归档 C19。

## 本次修正

- 不重开或改写 C23 历史归档。
- 根据真实讨论、提交、安装、Update 和验证证据建立独立 C24 恢复型归档。
- 明确标注 Phase 文档是事后压缩，不伪造原执行顺序。

## 可回流候选

- **原判断**：当前 Git 分支名可以帮助推断当前 Case。
- **纠偏**：Case 状态只以 `_aipd/case/index.md` 和 `case.md` 为准；分支名不是运行状态事实源。
- **候选规则**：当分支名指向已归档 Case、但用户开启了新的功能目标时，应在实现前创建新的递增 Case；可以暂时沿用分支，但必须显式记录 Case / branch 不一致。
- **候选门禁**：Close 时发现“用户说当前 Case，但索引里对应 Case 已归档”，不得选择唯一的其他 active Case；先按目标和 Git 证据识别是否需要新 Case或恢复型归档。

## 本轮归属

- 当前只留在 C24，未来需要时交给 `aipd-learn` 判断是否进入 `aipd-case` Skill、Close phase 或模板。
- 不写入普通项目 Map，不把一次流程偏差当成高频用户入口。
