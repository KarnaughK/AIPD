# Close：c31-cursor-leader-agent-runtime

## 闭环

- Think / Design / Execute / Verify 完成。
- 用户 2026-08-17 确认 install 并要求归档。
- Case Contract 目标已达成：Cursor 桌面端 Leader + 已登录 `cursor-agent` 执行层；DSH 退出现行合同。

## 归档候选 / 反向编织候选

| 内容 | 来源 | 状态 | 归属 | Close 判断 |
|---|---|---|---|---|
| Cursor 上 Leader 规划调度收口，执行层是 `cursor-agent`，`chatId` 记在 `_aipd/leader/` | Design / Execute / 用户确认 | 已完成可评估 | Core / Product / Engineering / map | 回写 |
| `install-cursor` 只写 `~/.cursor/skills/` | Execute / install | 已完成可评估 | Engineering / README | 回写 |
| Cursor 包不提目标模式 | 用户确认 | 已完成可评估 | Product / Engineering | 回写为现行边界 |
| DSH 曾作为实验执行层 | c29 / c30 | 历史 | 仅留 archive | 不回写为默认 |
| 过夜定时器 / Goal 循环 | 昨夜讨论 | 未做 | 仅留对话 | 不回写 |

## 长期认知审计

- 已回写：见 Close 后 Weave。
- 仅留 Case：DSH 实验过程、未做的扇出执行 Agent 与定时器。
- archive 决策：移动到 `_aipd/case/archive/c31-cursor-leader-agent-runtime/`。
