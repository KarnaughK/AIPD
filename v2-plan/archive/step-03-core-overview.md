# 第 3 步：新建 src/core/overview.md

## 任务

从现有 `SKILL.md` 提取框架总览，去掉所有流程性指令，只保留"AIPD 是什么"的知识描述。

## 要写入的文件

`src/core/overview.md`

## 内容要求

从当前 `SKILL.md`（根目录）提取以下部分，重新组织为纯知识文档：

1. **AIPD 是什么**：一段话描述框架定位
2. **核心原则**：经验总结型框架、_adoc/ 的本质
3. **五层认知结构简介**：L1-L5 每层一句话说明（不含详细指引，详细指引在各层自己的 guide.md 里）
4. **多 Agent 架构简介**：主 Agent = 项目负责人，子 Agent = 独立执行者（不含具体调用方式，那在 agent-team-guide.md 里）
5. **执行体系简介**：Plan 和 OKR 各一句话（不含具体流程）

**关键**：这个文件是给 `aipd` 总入口 skill 用的，让模型快速理解"AIPD 是什么"，然后引导用户去调用具体 skill。所以要短，不超过 80 行。

## 参考源文件

- `SKILL.md`（根目录，第 19-47 行的核心原则和五层结构部分）
- `v1/README.md`（项目说明中的架构描述）
