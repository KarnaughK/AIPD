interface:
  display_name: "AIPD Leader"
  short_description: "显式启动项目级 Leader，并按宿主调度 Case 执行层"
  default_prompt: "Use $aipd-leader to lead this project, clarify one Mission, and coordinate one Case runtime per Case."

policy:
  allow_implicit_invocation: false
