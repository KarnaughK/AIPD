# AIPD 上下文检索地图

本文件只负责引路，不承载完整正文。它把用户自然语言、业务词、工程词路由到 L3 / L4 / L5 / 局部 README / case。

## 使用规则

- 进入具体任务前，先用本文件判断应读哪些上下文。
- 命中不清楚时，先用 `rg` 搜索核心词、页面名、接口名、权限码、组件名和 README，再补充上下文包。
- 本文件记录高频入口和稳定路径，不记录一次性执行细节。
- 关键路径尽量扁平暴露，不要求 Agent 层层跳转后自行发现。

## 任务路由

| 用户意图 / 关键词 | 层级判断 | 必读上下文 | 代码 / 局部入口 | 兜底搜索 |
|---|---|---|---|---|
| {示例：合同 / 产品 / 任务 / 交付记录} | L3 + L4 | `_adoc/L3-core/index.md`、`_adoc/L4-product/index.md` | 相关页面或模块 README | `rg "合同|产品|任务|交付"` |
| {示例：权限 / 菜单 / 按钮显隐} | L5 + 局部 README | `_adoc/L5-dev/index.md` | 目标页面 README、权限工具函数 | `rg "permission|perms|auth|hasPermi"` |
| AIPD 架构升级 / 更新 AGENTS / 补 context-map | L5 + AIPD 项目入口 | `AGENTS.md`、`_adoc/index.md`、`_adoc/context-map.md` | AIPD update skill / 当前项目 AIPD 文档 | `rg "AIPD|context-map|上下文检索|AGENTS" .` |

## 核心概念入口

| 概念 | 标准叫法 | 相关文档 | 常见误解 / 别名 |
|---|---|---|---|
| {概念名} | {标准名} | `_adoc/L3-core/index.md` | {不要混用的词} |

## 工程规则入口

| 规则主题 | 适用范围 | 相关文档 | 代码入口 |
|---|---|---|---|
| {示例：若依权限} | 跨前后端、跨页面按钮权限 | `_adoc/L5-dev/index.md` | {权限工具 / 路由 / 菜单配置} |

## 自迭代观察锚点

后续用 `aipd2-learn` 审计会话或 transcript 时，观察本次任务是否遵守了这些锚点：

- Agent 是否先读取 `_adoc/index.md` 和本文件，再进入 L3 / L4 / L5 / 局部 README。
- Agent 是否明确输出或内部形成了上下文包：层级判断、必读文档、代码入口、兜底搜索词、风险边界。
- 如果任务涉及核心概念，Agent 是否读取 L3 概念索引，而不是直接猜业务含义。
- 如果任务涉及跨模块工程规则，Agent 是否读取 L5 工程实现规则，而不是只看局部页面代码。
- 如果任务涉及页面、弹窗或组件内部细节，Agent 是否读取就近 README，而不是把局部细节误塞进 L5。
- 如果 map 缺失或未命中，Agent 是否用 `rg` 兜底，并提出应回写到 map / index 的稳定入口。
