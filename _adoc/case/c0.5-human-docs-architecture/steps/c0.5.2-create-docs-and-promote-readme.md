# Step: c0.5.2 - 创建 docs 并升级 README

> **所属 Case**: c0.5-human-docs-architecture
> **类型**: dev / docs
> **推荐 Agent**: worker
> **依赖**: c0.5.1

## 目标

基于 `c0.5.1` 产出的信息架构方案，创建面向人的 `docs/` 学习文档，并完成 README 分工调整：将 `README-new.md` 升级为项目根 README，将旧 `README.md` 的展开说明迁移 / 改写到 docs。

## 已确认设计输入 / 执行依据

- 新 README 更适合作为 GitHub 首页 / 宣传页 / 落地页，用来讲 AIPD2 为什么存在、为什么值得继续看、项目大概在做什么。
- 旧 README 更适合降级为学习文档来源，用于展开讲 AIPD2 的设计思路、核心架构、模块职责和工程入口。
- README 首页必须提供明确入口跳到 `docs/`，否则用户不知道还有学习文档。
- 正式 docs 目录优先使用：
  - `docs/guide/`：新手入门流程。
  - `docs/modules/`：进阶模块说明。
- `guide/` 必须覆盖执行闭环，不能只讲知识库；AIPD2 是“项目知识库 + 任务执行组织”的综合体。
- 本 step 只产出 Markdown 文档，不执行构建、安装、测试或发布。

## 上下文文档

分身 Agent 必须在执行前读取以下文档：

- Case 总览：`_adoc/case/c0.5-human-docs-architecture/case.md`
- 信息架构 step：`_adoc/case/c0.5-human-docs-architecture/steps/c0.5.1-design-human-docs-ia.md`
- 信息架构 proposal：`_adoc/case/c0.5-human-docs-architecture/doc/docs-ia-proposal.md`
- 新 README 草稿：`README-new.md`
- 旧 README：`README.md`
- 项目地图：`_adoc/map.md`
- 核心认知：`_adoc/L3-core/index.md`
- 纵向概念模块：`_adoc/L3-core/vertical-concept-modules.md`
- 横向功能能力：`_adoc/L3-core/horizontal-capabilities.md`

## 任务清单

- [x] 创建正式 `docs/` 学习文档目录，优先包含 `docs/README.md`、`docs/guide/`、`docs/modules/`。
- [x] 将 `README-new.md` 内容升级为根目录 `README.md`，保留其作为 GitHub 首页 / 落地页的叙事风格。
- [x] 在新的根 `README.md` 开头或明显位置加入 `docs/` 学习文档入口链接，让读者能跳到 docs。
- [x] 将旧 `README.md` 中适合展开讲解的内容迁移 / 改写到 docs，而不是直接丢弃。
- [x] 按已确认方向生成 `guide/` 的入门流程文档，至少覆盖 AI Coding 演进、项目知识库、L1-L5、map、weave、case/step。
- [x] 按已确认方向生成 `modules/` 的进阶模块说明骨架，至少体现 Map / 检索系统双划分和上下文解耦核心概念。
- [x] 修正 README 中 `_adoc/context-map.md` 与当前 `_adoc/map.md` 不一致的问题。
- [x] 记录旧 README 迁移后的去向，避免内容失踪。

## 验收标准

- [x] 根目录 `README.md` 使用新版 README 的首页叙事，并能明显跳转到 docs。
- [x] 旧 README 的核心展开内容已进入 docs 的合适位置，或在迁移记录中说明删减原因。
- [x] `docs/guide/` 能作为新手入门路径，且包含 case / step 执行闭环。
- [x] `docs/modules/` 能作为进阶模块说明入口，且不写成源码级对象手册。
- [x] README / docs / _adoc 的职责没有混淆。
- [x] 本 step 只改 Markdown 文档，不执行构建安装。

## 不做

- 不改 AIPD2 skill 运行逻辑。
- 不执行 `scripts/build`、安装脚本或测试命令。
- 不把 `_adoc/` 改造成面向人的 docs。
- 不删除旧 README 的有效内容；需要删减时必须说明原因。

---

## 执行记录

> 分身 Agent 执行完成后填写此区域

**完成时间**：2026-06-07

**主要改动**：
- 将 `README-new.md` 的首页叙事升级为根目录 `README.md`，并在首页明显加入 `docs/` 学习文档入口。
- 创建 `docs/README.md`、`docs/guide/` 6 篇入门文档和 `docs/modules/` 9 篇进阶模块说明。
- 将旧 `README.md` 中的项目定位、工具关系、L1-L6、map、case / step、分身 Agent、skill、安装开发和项目结构内容迁移 / 改写到 docs。
- 将 README 中旧 `_adoc/context-map.md` 入口修正为当前 `_adoc/map.md`。

**遇到的问题**：
- 当前工作树在本 step 执行前已有多处非本 step 的未提交改动；本 step 未回滚或修改这些既有改动。
- `README-new.md` 保留为草稿文件，根 `README.md` 已使用其叙事升级；旧 README 的展开内容去向记录在 `docs/README.md`。

**Weave 候选**：
- `_adoc/map.md` 可新增“面向人类文档 / docs / README 分工”检索入口。
- 若 README / docs / _adoc 的职责分工稳定，可回写到 `_adoc/L5-dev/index.md` 或相关 L3 横向能力文档。
