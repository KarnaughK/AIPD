# Step: c0.1.2 - 让 Codex build/install 处理 custom agents

> **所属 Case**: c0.1-role-codex-custom-agents
> **类型**: dev
> **推荐 Agent**: worker
> **依赖**: c0.1.1

## 目标

让 Codex agent 模板从源码进入 dist，并能安装到用户级或项目级 Codex agents 目录。

## 上下文文档

- `scripts/build`
- `scripts/install-codex`
- `scripts/dev-codex`
- `scripts/install-project-codex`
- `src/platforms/codex/agents/aipd_vue_architect.toml`

## 任务清单

- [x] `scripts/build codex` 输出 `dist/codex/agents/`。
- [x] `scripts/install-codex` 安装 agents 到 `~/.codex/agents/`。
- [x] `scripts/dev-codex` 用软链安装 agents。
- [x] `scripts/install-project-codex` 安装 agents 到目标项目 `.codex/agents/`。

## 验收标准

- [x] `dist/codex/agents/aipd_vue_architect.toml` 存在。
- [x] `/Users/yangzongru/.codex/agents/aipd_vue_architect.toml` 存在。

## 不做

- 不实现独立 `aipd2-agent-init`。

---

## 执行记录

**完成时间**：2026-05-09

**主要改动**：
- 更新 Codex build/install/dev/project-install 脚本。
- 安装 `aipd_vue_architect` 到本机 Codex 用户级 agents 目录。

**遇到的问题**：无
