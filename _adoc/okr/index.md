# AIPD2 OKR 索引

## 当前目标

- **O1**：跑通 AIPD2 自举开发闭环。

## 当前关键结果

- **KR1**：AIPD2 仓库自身补齐 `_adoc/` 和 `AGENTS.md` 入口。
- **KR2**：Codex 子 Agent 默认上下文策略统一为 `fork_context: true`，并明确同源分身与结果回流模型。
- **KR3**：Codex custom agent 可被 case-run 识别和派发。
- **KR4**：`scripts/build codex` 与 `scripts/install-codex` 同时处理 skills 和 agents。
